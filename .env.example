# Redis连接配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# 服务器配置
PORT=3000

# 保活配置
RETRY_COUNT=2
RETRY_DELAY=2000

# 备用域名配置（如果Redis不可用）
TARGET_DOMAINS=["https://www.example.com"]

# 定时任务配置（cron表达式）
CRON_SCHEDULE=*/5 * * * *